import React, {useEffect} from 'react';
import LoginForm from "./LoginForm";

const Login = () => {
    // useEffect(() => {
    // }, []);
    return (
        <div>
            <LoginForm/>
        </div>

    );
};
export default Login;